# 宝可梦同人游戏翻译工具 v1.2.0

> 仓库：nevermore-glimpse/pkmn_translator ｜ 标签：v1.2.0

## 新增功能
- **亚克力毛玻璃 GUI**：圆形头像（去白圈）、宝可梦主题配色、8 个菜单页、右侧文件侧边栏、状态条小精灵球，支持本地 Ollama 与云端 API 双模式切换。
- **文件范围选择**：菜单 1/2/3/5 执行前可选「单文件 / 整个文件夹」，并自动跳过生成物（`*_translated.txt`、`*_translated_report.txt`）；选择结果在菜单间自动同步。
- **中文润色重翻**：新增菜单 5，对缓存中的中文译文做一轮润色并覆盖回写，不改变原意与占位符。
- **云端 API 模式**：除本地 Ollama 外，新增 OpenAI 兼容 `/chat/completions` 云端模式；切换时同步调整术语上限 / 超时 / 上下文 / 最大生成 / 每批条数 / 单批字符，并弹窗提示前后值。
- **检查更新**：从 GitHub Release 拉取最新 tag，与本地版本不一致时弹窗提醒（含下载链接）。
- **前缀 / 检查报告列表滚动条**：溢出时显示滚动条，长文件不溢出。

## 修复与改进
- **占位符保留加固（重点）**：
  - 术语表加载时排除 `@0@`/`⟦0⟧` 等占位符 token，杜绝其被当术语注入 prompt、反向教模型翻译占位符（根因修复）。
  - 清理 `term_dict.py` 中由模型误提取产生的 `@0@…@4@` 伪术语。
  - `auto_terms` 与 `@@T:` 提取增加占位符型 key 拒绝，防止再次污染。
  - 译后占位符校验失败时，针对性重试并明确告知模型「丢失了哪些 @N@」。
  - 兜底补回改用源文相对位置精确回插；并修正此前兜底结果被二次判失败、整条被丢弃的潜在缺陷。
  - 提示词补充短句占位符提醒与自校验。
- 术语提取 / 匹配性能优化（单词哈希 + 多词小正则）。
- 中文字体统一为微软雅黑，字号适度放大不溢出。
- 控制码回退：译文缺失/未保留的控制码按原文原样补回。

## 使用流程（简述）
1. 启动（Start）→ 设置 → 选择 Ollama 或 云端 API，填写模型/密钥。
2. 菜单 1 翻译：选文件/文件夹 → 翻译 → 自动生成检查报告。
3. 菜单 2 重翻检查报告：对问题句删除缓存并重翻。
4. 菜单 3 术语更新后重翻：术语表变动后仅重翻受影响句子。
5. 菜单 4 前缀字典：补全句首控制码译文后应用。
6. 菜单 5 中文润色：对译文做一轮润色重翻。
7. 菜单 6 从 Excel 生成术语表。
8. 菜单 8 日志 / 环境检查 / 检查更新。

## 升级说明
直接覆盖运行；首次使用请在「设置」中配置模型与（云端模式）API 密钥。术语表 `term_dict.py` 为本地私有数据，不随仓库分发。
